# Day12 详解 — 线程池

## 一、线程池概述

### 1.1 为什么使用线程池？

| 好处 | 说明 |
|------|------|
| **降低资源消耗** | 复用线程，减少创建销毁开销 |
| **提高响应速度** | 任务到达时，不需要等待线程创建 |
| **提高线程可管理性** | 统一管理、监控、调优 |

### 1.2 线程池工作原理图

```
                        ┌─────────────────────────────────────┐
                        │           线程池 (ThreadPool)        │
                        └─────────────────────────────────────┘
                                          │
                                          ▼
┌───────────────────┐    ┌─────────────────────────────────┐
│  任务提交         │───→│  1. 当前线程数 < corePoolSize?   │
└───────────────────┘    │     └─ 是: 创建核心线程执行       │
                         │     └─ 否: ↓                     │
                         │  2. 队列未满?                     │
                         │     └─ 是: 入队等待               │
                         │     └─ 否: ↓                     │
                         │  3. 当前线程数 < maximumPoolSize? │
                         │     └─ 是: 创建非核心线程执行     │
                         │     └─ 否: ↓                     │
                         │  4. 执行拒绝策略                   │
                         └─────────────────────────────────┘
```

---

## 二、ThreadPoolExecutor核心参数

### 2.1 七大核心参数

```java
public ThreadPoolExecutor(
    int corePoolSize,      // 核心线程数
    int maximumPoolSize,   // 最大线程数
    long keepAliveTime,    // 空闲线程存活时间
    TimeUnit unit,         // 时间单位
    BlockingQueue<Runnable> workQueue,  // 任务队列
    ThreadFactory threadFactory,        // 线程工厂
    RejectedExecutionHandler handler    // 拒绝策略
)
```

### 2.2 参数详解

| 参数 | 说明 | 建议值 |
|------|------|--------|
| **corePoolSize** | 核心线程数，一直存活 | CPU密集型: CPU核数+1<br>IO密集型: CPU核数*2 |
| **maximumPoolSize** | 最大线程数 | 根据业务调整 |
| **keepAliveTime** | 空闲线程存活时间 | 60s |
| **unit** | 时间单位 | TimeUnit.SECONDS |
| **workQueue** | 任务队列 | LinkedBlockingQueue |
| **threadFactory** | 线程工厂 | 自定义线程名 |
| **handler** | 拒绝策略 | ThreadPoolExecutor.AbortPolicy |

### 2.3 代码示例

```java
import java.util.concurrent.*;

public class ThreadPoolDemo {
    public static void main(String[] args) {
        ThreadPoolExecutor executor = new ThreadPoolExecutor(
            5,                          // 核心线程数
            10,                         // 最大线程数
            60L,                        // 空闲时间
            TimeUnit.SECONDS,           // 时间单位
            new LinkedBlockingQueue<>(100),  // 任务队列
            Executors.defaultThreadFactory(),  // 线程工厂
            new ThreadPoolExecutor.AbortPolicy()  // 拒绝策略
        );
        
        // 提交任务
        executor.execute(() -> System.out.println("任务执行"));
        
        executor.shutdown();
    }
}
```

---

## 三、任务执行流程

### 3.1 execute()流程详解

```java
public void execute(Runnable command) {
    int c = ctl.get();
    
    // 1. 当前线程数 < 核心线程数，创建核心线程
    if (workerCountOf(c) < corePoolSize) {
        if (addWorker(command, true))
            return;
        c = ctl.get();
    }
    
    // 2. 核心线程已满，尝试入队
    if (isRunning(c) && workQueue.offer(command)) {
        int recheck = ctl.get();
        if (!isRunning(recheck) && remove(command))
            reject(command);
        else if (workerCountOf(recheck) == 0)
            addWorker(null, false);
    }
    // 3. 队列已满，创建非核心线程
    else if (!addWorker(command, false))
        // 4. 创建失败，执行拒绝策略
        reject(command);
}
```

### 3.2 流程图解

```
任务提交
    │
    ▼
┌─────────────────┐     ┌─────────────────┐
│ 当前线程数       │  否  │ 创建核心线程     │
│ < corePoolSize? │────→│ 执行任务         │
└────────┬────────┘     └─────────────────┘
         │ 是
         ▼
┌─────────────────┐     ┌─────────────────┐
│  队列是否已满?   │  否  │ 入队等待         │
└────────┬────────┘────→└─────────────────┘
         │ 是
         ▼
┌─────────────────┐     ┌─────────────────┐
│ 当前线程数       │  否  │ 创建非核心线程   │
│ < maximumSize?  │────→│ 执行任务         │
└────────┬────────┘     └─────────────────┘
         │ 是
         ▼
┌─────────────────┐
│ 执行拒绝策略     │
└─────────────────┘
```

---

## 四、四种拒绝策略

### 4.1 策略对比

| 策略 | 行为 | 适用场景 |
|------|------|---------|
| **AbortPolicy** | 抛出RejectedExecutionException | 默认策略，允许任务丢失 |
| **CallerRunsPolicy** | 由提交任务的线程执行 | 不希望任务丢失 |
| **DiscardPolicy** | 静默丢弃任务 | 允许任务丢失 |
| **DiscardOldestPolicy** | 丢弃队列最旧的任务 | 允许任务丢失 |

### 4.2 代码演示

```java
// AbortPolicy（默认）
ThreadPoolExecutor executor1 = new ThreadPoolExecutor(
    1, 1, 0L, TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(1),
    new ThreadPoolExecutor.AbortPolicy()
);

// CallerRunsPolicy
ThreadPoolExecutor executor2 = new ThreadPoolExecutor(
    1, 1, 0L, TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(1),
    new ThreadPoolExecutor.CallerRunsPolicy()
);

// DiscardPolicy
ThreadPoolExecutor executor3 = new ThreadPoolExecutor(
    1, 1, 0L, TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(1),
    new ThreadPoolExecutor.DiscardPolicy()
);

// DiscardOldestPolicy
ThreadPoolExecutor executor4 = new ThreadPoolExecutor(
    1, 1, 0L, TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(1),
    new ThreadPoolExecutor.DiscardOldestPolicy()
);
```

### 4.3 自定义拒绝策略

```java
public class CustomRejectionHandler implements RejectedExecutionHandler {
    @Override
    public void rejectedExecution(Runnable r, ThreadPoolExecutor executor) {
        // 自定义处理逻辑
        System.out.println("任务被拒绝: " + r.toString());
        // 可以记录日志、发送告警等
    }
}

// 使用
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    1, 1, 0L, TimeUnit.MILLISECONDS,
    new ArrayBlockingQueue<>(1),
    new CustomRejectionHandler()
);
```

---

## 五、execute() vs submit()

### 5.1 核心区别

| 对比项 | execute() | submit() |
|--------|-----------|----------|
| **返回值** | 无 | Future |
| **异常处理** | 直接抛出 | 封装在Future中 |
| **任务类型** | Runnable | Runnable/Callable |
| **适用场景** | 不需要返回值 | 需要返回值或处理异常 |

### 5.2 代码对比

```java
// execute() - 无返回值
executor.execute(() -> {
    System.out.println("execute任务");
});

// submit() - 有返回值
Future<Integer> future = executor.submit(() -> {
    return 1 + 1;
});

// 获取结果
try {
    Integer result = future.get();  // 阻塞等待
    System.out.println("结果: " + result);
} catch (InterruptedException | ExecutionException e) {
    e.printStackTrace();
}
```

### 5.3 异常处理对比

```java
// execute() - 异常直接抛出
executor.execute(() -> {
    throw new RuntimeException("execute异常");
});
// 主线程会收到异常

// submit() - 异常封装在Future中
Future<?> future = executor.submit(() -> {
    throw new RuntimeException("submit异常");
});
// 主线程不会收到异常，需要调用get()才会抛出
try {
    future.get();
} catch (ExecutionException e) {
    System.out.println("捕获异常: " + e.getCause());
}
```

---

## 六、线程池大小确定

### 6.1 经验公式

```
CPU密集型任务:
线程数 = CPU核数 + 1

IO密集型任务:
线程数 = CPU核数 * (1 + IO等待时间/CPU执行时间)

或者:
线程数 = CPU核数 * 2
```

### 6.2 任务类型判断

```java
// CPU密集型：计算密集，CPU占用高
// 例如：加密解密、图像处理、算法计算

// IO密集型：IO操作多，CPU空闲等待
// 例如：数据库操作、文件读写、网络请求

// 混合型：根据IO等待时间占比判断
```

### 6.3 动态获取CPU核数

```java
// 获取CPU核数
int cpuCores = Runtime.getRuntime().availableProcessors();

// 或使用
int cpuCores = ProcessorInfo.getNumberOfProcessors();
```

---

## 七、常用线程池

### 7.1 Executors工厂方法

```java
// 1. 固定线程数线程池
ExecutorService fixedPool = Executors.newFixedThreadPool(5);

// 2. 单线程线程池
ExecutorService singlePool = Executors.newSingleThreadExecutor();

// 3. 缓存线程池（按需创建）
ExecutorService cachedPool = Executors.newCachedThreadPool();

// 4. 定时线程池
ScheduledExecutorService scheduledPool = Executors.newScheduledThreadPool(5);
```

### 7.2 各种线程池特点

| 线程池 | 核心线程数 | 最大线程数 | 队列 | 适用场景 |
|--------|-----------|-----------|------|---------|
| **Fixed** | 固定 | 固定 | LinkedBlockingQueue | 固定并发量 |
| **Single** | 1 | 1 | LinkedBlockingQueue | 顺序执行 |
| **Cached** | 0 | Integer.MAX | SynchronousQueue | 大量短任务 |
| **Scheduled** | 固定 | Integer.MAX | DelayedWorkQueue | 定时任务 |

### 7.3 阿里巴巴规范禁用Executors的原因

```java
// 问题1: FixedThreadPool和SingleThreadPool
// 允许的请求队列长度为Integer.MAX_VALUE，可能堆积大量任务导致OOM

// 问题2: CachedThreadPool
// 允许的创建线程数量为Integer.MAX_VALUE，可能创建大量线程导致OOM

// 正确做法：手动创建ThreadPoolExecutor
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    5,                          // 核心线程数
    10,                         // 最大线程数
    60L,                        // 空闲时间
    TimeUnit.SECONDS,
    new LinkedBlockingQueue<>(1000),  // 有界队列
    Executors.defaultThreadFactory(),
    new ThreadPoolExecutor.AbortPolicy()
);
```

---

## 八、线程池监控

### 8.1 常用监控方法

```java
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    5, 10, 60L, TimeUnit.SECONDS,
    new LinkedBlockingQueue<>(1000)
);

// 获取线程池状态
executor.getActiveCount();      // 活跃线程数
executor.getPoolSize();         // 当前线程数
executor.getQueue().size();     // 队列任务数
executor.getCompletedTaskCount(); // 已完成任务数
executor.getTaskCount();        // 总任务数
executor.isShutdown();          // 是否已关闭
executor.isTerminated();        // 是否已终止
```

### 8.2 自定义监控

```java
public class ThreadPoolMonitor {
    private final ThreadPoolExecutor executor;
    private final ScheduledExecutorService monitor = 
        Executors.newSingleThreadScheduledExecutor();
    
    public ThreadPoolMonitor(ThreadPoolExecutor executor) {
        this.executor = executor;
        // 每秒打印一次状态
        monitor.scheduleAtFixedRate(() -> {
            System.out.println("活跃线程: " + executor.getActiveCount());
            System.out.println("队列大小: " + executor.getQueue().size());
            System.out.println("完成任务: " + executor.getCompletedTaskCount());
        }, 0, 1, TimeUnit.SECONDS);
    }
}
```

---

## 九、LeetCode题目解析

### 9.1 [1118. Number of Days in a Month（Easy）](https://leetcode.cn/problems/number-of-days-in-a-month/)

**题目描述**：
给定年份和月份，返回该月有多少天。

**思路分析**：
这道题主要考察Calendar类的使用，属于热身题。

```java
class Solution {
    public int numberOfDays(int year, int month) {
        Calendar calendar = Calendar.getInstance();
        calendar.set(year, month - 1, 1);
        return calendar.getActualMaximum(Calendar.DAY_OF_MONTH);
    }
}
```

**复杂度分析**：
- 时间复杂度：O(1)
- 空间复杂度：O(1)

---

## 十、今日学习要点总结

### 核心要点归纳

1. **七大核心参数**：corePoolSize、maximumPoolSize、keepAliveTime、unit、workQueue、threadFactory、handler
2. **任务执行流程**：核心线程 → 队列 → 非核心线程 → 拒绝策略
3. **四种拒绝策略**：AbortPolicy、CallerRunsPolicy、DiscardPolicy、DiscardOldestPolicy
4. **execute() vs submit()**：execute无返回值，submit有返回值
5. **线程池大小**：CPU密集型=CPU核数+1，IO密集型=CPU核数*2
6. **阿里规范**：禁用Executors，手动创建ThreadPoolExecutor

### 验收清单

- [ ] 能解释execute()流程（核心线程→队列→最大线程→拒绝）
- [ ] 能说出线程池大小的经验公式
- [ ] 能解释为什么阿里规范禁用Executors创建线程池
- [ ] 能说出四种拒绝策略的区别
- [ ] 能解释execute()和submit()的区别

---

## 十一、练习建议

1. **参数记忆**：熟练掌握七大参数的含义和建议值
2. **源码阅读**：阅读ThreadPoolExecutor的execute()源码
3. **拒绝策略实践**：实现自定义拒绝策略
4. **监控实践**：实现线程池监控，打印关键指标

---

## 十二、扩展阅读

- 《Java并发编程的艺术》第8章
- 《Java并发编程实战》第8章
- ThreadPoolExecutor源码分析
